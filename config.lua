Config = {
    AutoCheckVersion = true, -- Check version of the script everytime it get started

    CommandToClaimLog = 'magic', -- Commando to receive a complete log about a player - USE: /magic (Player ID)
    CommandCheckItem = 'checkitem', -- Command to check and specific item in the inventory - USE: /checkitem (Player ID) (Item to Search)

    ServerCurrency = '$', -- You can select the currency you use in your server for the message be correct
    -- Message Details
    WebHook = 'https://discord.com/api/webhooks/?????', -- Insert your webhook link
    SenderName = 'Command Logger', -- Name for the message sender
    EmbedColor = 16756736, -- Embed Color
}